{
  "name": "poker-trainer",
  "version": "1.0.0",
  "private": true,
  "description": "A mobile poker trainer: play hold'em against bots and have every decision graded.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "node scripts/serve.mjs",
    "icons": "node scripts/make-icons.mjs",
    "ui-check": "node scripts/ui-check.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
